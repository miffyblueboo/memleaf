# Hermes integration source

The single maintained Hermes Provider implementation, manifest, and usage
documentation live in `src/memleaf/hermes_provider/`.

Installers and tests use that packaged source directly. Do not add a second
Python Provider implementation or manifest in this directory.
